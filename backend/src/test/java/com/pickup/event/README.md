# event (tests)

No test files directly in this package — coverage lives in the subpackages
below.

- [`assignment/`](assignment/README.md) — `AssignmentService` (manual/auto trip
  persistence).
- [`planning/`](planning/README.md) — `DriverPassengerScorer` and
  `StopOrderPlanner` (auto-assignment heuristics).
